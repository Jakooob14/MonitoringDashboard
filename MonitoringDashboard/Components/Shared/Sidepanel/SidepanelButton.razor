@inject NavigationManager Nav

<NavLink 
    class=@($"flex flex-row items-center gap-2 hover:bg-zinc-900 transition-colors w-full p-3 rounded-sm cursor-pointer text-zinc-100 {(_active ? "bg-zinc-900" : "")}")
    Href="@Href">
    <Icon Path="@IconPath" class="@($"transition-colors {(_active ? "text-aero-500" : "")}")" /> @Text
</NavLink>